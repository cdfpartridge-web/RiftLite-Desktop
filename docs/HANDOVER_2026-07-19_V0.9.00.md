# RiftLite v0.9.00 Release Handover — 2026-07-19

This is the current continuation point for RiftLite engineering. It supersedes `docs/HANDOVER_2026-07-17.md` for local work while preserving that document as the published v0.8.04 reference.

## Release Status

- The approved UI development build has been promoted into the canonical desktop repository as RiftLite `v0.9.00` (`package.json` SemVer `0.9.0`).
- The canonical source remains:
  `C:\Users\cdfpa\OneDrive\Documents\Claude\Projects\Riftlite Beta 0.6\desktop-v06`
- RiftLite `v0.9.00` is published for Windows: `https://github.com/cdfpartridge-web/RiftLite-Desktop/releases/tag/v0.9.00`.
- RiftLite `mac-v0.9.00` is published for Intel and Apple Silicon macOS: `https://github.com/cdfpartridge-web/RiftLite-Desktop-mac/releases/tag/mac-v0.9.00`.
- Release source commit `f46e12d` and the macOS release-workflow fix `5be33c8` are on both desktop repositories' `main` branches.
- Website PR `https://github.com/cdfpartridge-web/Riftlite/pull/1` merged as `d35ab00`. Vercel production deployment `dpl_AsbjQNoboHZPvRtZUnAGCuazuoTH` is Ready and owns the public aliases.
- Firestore production release `cloud.firestore` points to ruleset `projects/riftlite-b61a5/rulesets/a426d034-68e7-482a-9bec-dfb047f3b33e`; rollback predecessor: `projects/riftlite-b61a5/rulesets/ad796f25-7d05-4d3e-9431-d8b1e1092ffe`.

## Production Continuity Invariants

v0.9.00 is an in-place upgrade of the normal app, not the former side-by-side UI Dev application. Preserve these values in future work:

- Electron app ID: `com.riftlite.desktop.beta06`
- Existing user-data directory: `%APPDATA%\RiftLite Beta 0.6`
- Existing media directory name under Pictures/Documents: `RiftLite`
- Deep-link protocol: `riftlite://`
- Windows release output directory: `release`
- Windows installer filename: `RiftLiteBetaInstall.exe`
- macOS artifact pattern: `RiftLiteBetaInstall-${arch}.{dmg,zip}`
- Updater and anonymized usage analytics remain enabled in production.
- Production account-sync defaults remain `community-and-hubs` with community sync enabled.
- Local packaging scripts use `--publish never`; the verified artifacts were attached to GitHub only through the intentional release workflow.

Do not derive media folders from the new display name. The fixed `RiftLite` media directory is required so existing replay videos, bundles, screenshots, and backups continue resolving.

## What v0.9.00 Contains

- The approved modern visual system across Home, Play, Review, Prepare, Community, Overlay, Account, Settings, and supporting dialogs.
- Collapsible navigation retained, including coordinated Play-toolbar resizing.
- The Play toolbar exposes the force-stop-match action directly.
- Settings are grouped behind accordion sections.
- Home retains marketing inventory, sponsor contact, the themed community spotlight carousel, Riftlab placement, active-deck presentation, and legend artwork.
- First-launch guidance continues through the user's first successfully saved resolved match.
- Atlas empty-shell recovery without clearing sign-in/session storage, plus viewport and card-rendering fixes.
- Match popup scrolling and Replay V2 presentation fixes.
- Alternate-art, signed, and overnumbered card normalization for tracking and replay presentation.
- Expanded battlefield/card registry data with guarded generation and runtime validation.
- Replay media attachment and missing-media recovery improvements.
- Simplified email/Google-linked account flow, canonical identity repair, cloud restore coordination, and active-deck/history restoration.
- Discord active-deck reporting posts the Piltover deck link when the legend matches.
- Community spotlight cards open the creator's RiftLite page.

## Account Confidence, Security, and Privacy

- Account Sync shows local/cloud match and deck counts, last successful sync, active-deck state, data-source/device time, and migration progress.
- Choosing local or cloud data presents a preview before applying the choice.
- Account and two-device conflict tests are mandatory release-gate checks.
- Refresh tokens and integration secrets migrate to Electron secure storage: Windows DPAPI or macOS Keychain-backed storage.
- Newly created manual and cloud backups omit secrets by default.
- Diagnostic bundles redact names, handles, email addresses, room codes, tokens, raw payloads, and local paths unless a separate sensitive export is explicitly confirmed.
- App IPC validates the trusted renderer origin. Embedded Atlas and TCGA guests use narrow provider, partition, navigation, popup, permission, and payload policies.

Existing retained SQLite recovery `.bak` files are not automatically rewritten or deleted. A backup created before this hardening can retain an old plaintext value until normal ten-backup rotation replaces it or the user deliberately removes it. Newly created manual and cloud backups are sanitized.

## Web Replay 6× and 10×

The website workspace has deployed, locally uncommitted Web Replay support for `1×`, `2×`, `4×`, `6×`, and `10×` in:

- `src/components/replay-v2/ReplayV2Player.tsx`
- `src/components/replay-v2/ReplayV2Player.test.ts`

The compact control cycles all five speeds; the More panel exposes them directly; keyboard `6` selects 6× and keyboard `0` selects 10×. Shortcut help and animation-scaling regression coverage were updated.

This is website-owned functionality because the desktop embeds `https://www.riftlite.com/replays/embed?embed=1`. Deployment `dpl_81uCmadSXxGnKvLdGTkrcdXMFg7h` made 6×/10× available to both browser and desktop embedded Web Replay without publishing the v0.9.00 desktop installer.

The website Home download copy now advertises `v0.9.00`, and its Windows/macOS download links resolve through the corresponding GitHub Latest releases.

## Validation Completed

- Desktop lint and TypeScript passed.
- Mandatory account/two-device gate passed: 5 files, 49 tests.
- Full desktop suite passed: 68 files, 552 tests.
- After the macOS workflow-order regression test was added, the final shared release gate passed 68 files and 553 tests on Windows and GitHub macOS CI.
- Electron main, game preload, and Vite production builds passed.
- Registry check passed: 1,178 unique prints, 1,178 Riot image hashes, and seven local special battlefields.
- Source and packaged registries are byte-identical: 942,104 bytes; SHA-256 `ED800FA9F8C325B11AB212C55113384083C71A118EF25D97B44E5825ED5BD957`.
- Website suite passed: 54 files passed and 1 skipped; 366 tests passed and 9 skipped.
- Firestore emulator rules suite passed: 9 tests.
- Website lint passed with zero errors and 15 existing warnings.
- Website production build passed, retaining known non-fatal Next cache warnings for entries larger than 2 MB.
- Vercel deployment `dpl_AsbjQNoboHZPvRtZUnAGCuazuoTH` is Ready and owns the `www.riftlite.com`, `riftlite.com`, and `riftlite.vercel.app` production aliases.
- Live smoke checks returned 200 for Home, app configuration, Replays, and the embedded replay shell. The new hub lifecycle and Web Replay routes rejected unauthenticated requests as expected. A real public replay exposed 6× and 10× in the More panel, and keyboard `6`/`0` selected them correctly.
- The deployed Firestore rules content SHA-256 matches local exactly: `E3C02F8C10489F3AF42CD4BA9FC90BFE9FAB7ED9F7E4E1D77EF8A4B7AC7F7447`.
- Desktop and website `git diff --check` passed apart from informational Windows line-ending notices.

## Installer

Validated local Windows installer:

`C:\Users\cdfpa\OneDrive\Documents\Claude\Projects\Riftlite Beta 0.6\desktop-v06\release\RiftLiteBetaInstall.exe`

- Size: 200,612,562 bytes
- SHA-256: `4E1FF08E4630D86646526B59ECF54EC91DBB31750AB4F2E770CDD4CC5F63C97A`
- Product name: `RiftLite Beta 0.9`
- Product/file version: `0.9.0`
- NSIS archive integrity: passed

Adjacent release metadata:

- `RiftLiteBetaInstall.exe.blockmap`: 186,890 bytes; SHA-256 `1479318D27DDE923F23ECAD8CD9B5BDEAA263372A08E47A609A44584D5509271`
- `latest.yml`: 343 bytes; SHA-256 `011D7852E39600BD21A4BBADF110C2BA588879F850296D0759B0DB264B0A5BE4`
- `latest.yml` reports `0.9.0`, and its installer size and SHA-512 match the built artifact.
- GitHub's public size and SHA-256 digest for all three Windows assets match the local release artifacts exactly.

Published Windows release:

`https://github.com/cdfpartridge-web/RiftLite-Desktop/releases/tag/v0.9.00`

## macOS Installers

GitHub Actions run `https://github.com/cdfpartridge-web/RiftLite-Desktop-mac/actions/runs/29705150387` passed the release gate, production build, Intel/Apple Silicon packaging, and artifact upload.

- `RiftLiteBetaInstall-arm64.dmg`: 165,435,603 bytes; SHA-256 `A0164388ADE5345D65B1816A2651DA3EFF6CB1B4B640950B6CD41A8D15F94365`
- `RiftLiteBetaInstall-arm64.zip`: 158,194,438 bytes; SHA-256 `478EC1C7F08786DE4E9F5B73D2122C01CAE2886CE22A0B4165C528DAFAA5B327`
- `RiftLiteBetaInstall-x64.dmg`: 173,687,003 bytes; SHA-256 `25C0D5EE80DFCA1CA05F7975A167115A1D41B1AA6FB8A2955F00487CDF79B479`
- `RiftLiteBetaInstall-x64.zip`: 166,394,708 bytes; SHA-256 `D89DF6827B04A9A524096A8F5B58B56D4832FC031DD1E3B5B9705F61D6EB0EB1`
- `latest-mac.yml`: 829 bytes; SHA-256 `C034CFDE0C9C84FFB01A35B8C46F3D31C61CE2CD4F19BB5CD84AA231CE18EE4E`
- Every `latest-mac.yml` size and SHA-512 entry matches its built artifact, and every public GitHub asset size/SHA-256 digest matches the downloaded CI artifact.

Published macOS release:

`https://github.com/cdfpartridge-web/RiftLite-Desktop-mac/releases/tag/mac-v0.9.00`

## Packaged Smoke Results

- A fresh isolated profile launched and exited cleanly, created its database and secure vault, and rendered the first-launch guided tour.
- A completed-tour profile rendered the modern Home dashboard with `v0.9.00`, no UI Dev labels, the sponsor panel, Riftlab/community carousel, active deck, and first-match onboarding.
- A disposable copy of the real production profile launched cleanly while the original profile remained hash-identical and untouched.
- That copied profile retained 402 matches, 280 replays, 25 saved decks, account identity, and active deck.
- Three legacy settings secrets migrated into secure storage, and a complete scan of the disposable profile found no remaining plaintext copies.

## Atlas Embedded Sign-in Hotfix

On 2026-07-19 the rebuilt v0.9.00 candidate exposed a Clerk `authorization_invalid` response after starting RiftAtlas sign-in inside RiftLite. The final installed retry was correlated against the app capture timestamps and a read-only copy of Opera GX history. At the exact failure time, `accounts.google.com` and `https://clerk.riftatlas.com/v1/oauth_callback` ran in Opera rather than RiftLite. The temporary history copy was then removed; the real browser profile was not changed.

- RiftAtlas stores Clerk's required client state in cookies under `persist:riftlite-atlas`; the provider callback must run in that same Electron session.
- The root cause was a split navigation policy. Approved OAuth origins were retained for `window.open` popups, but Clerk currently performs Google sign-in as a same-window redirect. RiftLite's main-frame guard blocked that redirect and explicitly handed it to the default browser. Opera did not have the Clerk client state from the embedded Atlas partition, so Clerk correctly returned HTTP 403 `authorization_invalid`.
- `isAllowedGameMainFrameNavigation` now keeps only the exact approved default-port HTTPS game/OAuth origins inside the persistent game session. Unknown origins, non-default ports, cross-provider callbacks and `about:blank` remain blocked. Unrelated external links retain the normal external-browser behavior.
- Popup OAuth continues to share the exact parent session with sandboxing, context isolation, disabled Node integration, web security and the narrow provider allowlist.
- Exact Clerk callback failure recovery now covers both popup and same-window flows. It detects only HTTP 403 at the exact Clerk `/v1/oauth_callback` path, with the exact Clerk JSON error parser retained as a fallback.
- Recovery clears only Atlas Clerk authentication cookies (`__client*`, `__session*`, `__refresh*`, and `__clerk*`), preserving Cloudflare and unrelated Atlas cookies. RiftLite account data, matches, decks and replays are untouched.
- Focused popup/content-security coverage passed 13 tests. The final complete release gate passed 68 files / 552 tests, including the separate 49-test account/two-device gate, TypeScript, production builds, and NSIS packaging.
- Final local installer: 200,612,562 bytes; SHA-256 `4E1FF08E4630D86646526B59ECF54EC91DBB31750AB4F2E770CDD4CC5F63C97A`. `latest.yml` reports `0.9.0`, and its size and SHA-512 match the installer. NSIS archive integrity passed.

## Atlas Local Video and Replay-State Hotfix

The final release candidate also fixes the contradictory state reported by replay `rl2_41f5854faf062d7828dcd00b9c104fa1`: the server replay was Ready and Discord had posted it, while the open desktop detail panel continued to show an earlier pre-upload snapshot.

- `RawCaptureService` now emits each saved replay transition to the trusted renderer, and the renderer replaces the existing list record without changing its order.
- Delivery copy distinguishes Preparing, Queued, Uploaded, Ready, and Shared states. A ready Web Replay is no longer labelled Missing media merely because optional local video was unavailable.
- Electron display-media authorization now compares the stable process/routing identity of the trusted top-level frame instead of JavaScript wrapper identity. The old comparison caused Chromium `AbortError: Error starting capture` even with recording enabled.
- If direct Atlas game-frame recording still cannot start, RiftLite falls back to the existing cropped system-window recorder.
- The exact retained raw capture reconstructed 319 deterministic board frames with no engine diagnostics; the already-missed local MP4 cannot be recreated after the match, but the Web Replay and raw capture remain intact.
- Focused regression tests, lint, all 552 desktop tests, the production build, and the final NSIS installer completed successfully after these fixes.

## Known Caveats and Next Checks

1. Complete one post-release installed `0.8.x → 0.9.00` upgrade smoke. Automated isolated/profile-copy upgrades passed, but the real installed-app upgrade was not performed before publication because it would mutate the user's installed application and protocol registration.
2. Complete one real Atlas OAuth sign-in and one TCGA sign-in on the published build, then capture one match and confirm both local recording and Web Replay delivery.
3. Repeat the automated two-device account-conflict scenarios on two physical devices when available.
4. Windows remains unsigned and macOS is ad-hoc signed/not notarized, so operating-system trust warnings remain expected.

The bulk promotion copy left 38 tracked paths appearing modified because their worktree copies use CRLF while the Git blobs use LF. Their clean-filtered hashes match the index and they do not appear in `git diff`; they are metadata/line-ending false positives, not content drift. Leave them alone unless a clean status is required. If so, refresh only the individually confirmed paths with `git add --refresh -- <paths>`; never use a broad `git add` in this dirty worktree.
